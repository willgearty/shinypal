% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_step.R
\name{add_shinypal_data_step}
\alias{add_shinypal_data_step}
\title{Register a complete data-producing step}
\usage{
add_shinypal_data_step(
  ind,
  data,
  fun_workflow,
  fun_report,
  libs = character(0),
  code_guard = NULL,
  ec_subs = NULL,
  select_dataset = FALSE,
  column_ids = character(0),
  rename = TRUE
)
}
\arguments{
\item{ind}{The index of the step.}

\item{data}{A \code{\link[shinymeta:metaReactive2]{shinymeta::metaReactive2()}} object produced by the step. It
should use \code{varname = paste0("occs_", ind)} so its generated variable name
matches the stored name.}

\item{fun_workflow}{A function that generates the UI elements for the
workflow.}

\item{fun_report}{A function that generates the UI elements for the report.}

\item{libs}{A character vector of R packages required for this step.}

\item{code_guard}{An optional zero-argument function evaluated inside the
code output before \code{\link[=get_chunk]{get_chunk()}}. Use it to surface step-specific
\code{validate()} messages; plain \code{req()}s inside \code{data} already propagate
through \code{\link[=get_chunk]{get_chunk()}}, so simple steps can leave this \code{NULL}.}

\item{ec_subs}{An optional list of length 2, where the first element is a
metaReactive object and the second element is a callback function. This is
used to substitute expansion contexts in the code chain.}

\item{select_dataset}{Whether the step consumes an upstream dataset (and thus
needs a \code{\link[=df_select_observe]{df_select_observe()}} to keep its dataset dropdown current).}

\item{column_ids}{A character vector of column-selector input ids to keep in
sync via \code{\link[=column_select_observe]{column_select_observe()}} (one per \code{\link[=select_column_input]{select_column_input()}}).}

\item{rename}{Whether to render a \code{\link[=varname_input]{varname_input()}} text field in the step's
panel (and install \code{\link[=var_name_observe]{var_name_observe()}}) so the user can give this step's
dataset a custom name, used as its label in later selectors and its
variable name in the generated script. Defaults to \code{TRUE}.}
}
\value{
Called for its side effects and returns \code{NULL} invisibly. Stores the
step's data reactive under \verb{occs_<ind>}, wires its code output and
data-preview modal, registers the step via \code{\link[=add_shinypal_step]{add_shinypal_step()}}, and (when
requested) installs the dataset/column selector observers and, when
\code{rename = TRUE}, the dataset-rename field and its observer.
}
\description{
Convenience wrapper around the boilerplate shared by every step that
produces an intermediate dataset: it stores the data reactive, wires
the data-preview modal, registers the step via \code{\link[=add_shinypal_step]{add_shinypal_step()}},
and (optionally) keeps the dataset dropdown and column selectors in
sync. A module only supplies the \code{data} reactive and its UI/report
functions.
}
\examples{
\dontrun{
# inside a module's server.R
ind <- next_step_index()
occs <- shinymeta::metaReactive2(varname = paste0("occs_", ind), {
  shinymeta::metaExpr(head(mtcars, input[[paste0("n_", ind)]]))
})
add_shinypal_data_step(
  ind, data = occs,
  fun_workflow = function(ind) accordion_panel_remove_button(ind, "Subset"),
  fun_report = function(ind) verbatimTextOutput_copy(ind)
)
}
}
\seealso{
Other workflow steps:
\code{\link[=add_shinypal_plot_step]{add_shinypal_plot_step()}},
\code{\link[=add_shinypal_step]{add_shinypal_step()}},
\code{\link[=next_step_index]{next_step_index()}}
}
\concept{workflow steps}
