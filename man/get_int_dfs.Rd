% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/server-utils.R
\name{get_int_dfs}
\alias{get_int_dfs}
\title{Get the names of all intermediate data.frames for a given step}
\usage{
get_int_dfs(ind)
}
\arguments{
\item{ind}{The index of the step.}
}
\value{
A named character vector of intermediate datasets in workflow
order: values are the stable internal ids (\verb{occs_<ind>}) and names are the
display labels (a custom name if set, otherwise the id). Empty if none are
available upstream.
}
\description{
Returns the names of intermediate datasets produced by steps \emph{earlier} than
\code{ind} in the current workflow order, so a step can only ever consume
upstream output. Used to populate the dataset selectors.
}
\examples{
\dontrun{
# inside a reactive or observer in a module, after shinypal_setup()
get_int_dfs(ind)
}
}
\seealso{
Other intermediate data:
\code{\link[=get_int_data]{get_int_data()}},
\code{\link[=set_int_data]{set_int_data()}}
}
\concept{intermediate data}
