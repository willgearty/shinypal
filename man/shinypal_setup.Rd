% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/setup.R
\name{shinypal_setup}
\alias{shinypal_setup}
\title{Setup shinypal}
\usage{
shinypal_setup(
  input,
  output,
  session,
  modules,
  download_filename = "shinypal_script.zip",
  download_template = "./modules/test_report.qmd",
  prefix = "data_"
)
}
\arguments{
\item{input}{The shiny input object.}

\item{output}{The shiny output object.}

\item{session}{The shiny session object.}

\item{modules}{A character vector of paths to independent modules.}

\item{download_filename}{The name of the file to download when the user
clicks the download button.}

\item{download_template}{The path to the Quarto markdown template file that
will be used to generate the report.}

\item{prefix}{Prefix for each data step's internal id and the variable name it
gets in the generated script (default \code{"data_"}). Use \code{\link[=step_varname]{step_varname()}} to
retrieve the full value for a given step index.}
}
\value{
Called for its side effects and returns \code{NULL} invisibly. It
initializes shinypal's shared reactive state, renders the live report and
wires the download handler and workflow observers, then sources each
module's \code{ui-aux.R} and \code{server.R} so their steps become available.
}
\description{
A function to set up the shinypal environment. This function should be
called at the top of the server function of your shiny app.
}
\details{
Each module should have a \code{ui-aux.R} file that defines the UI elements for
that module in the "Workflow" accordion and a \code{server.R} file that defines
the server-side logic for the module.
}
\examples{
\dontrun{
server <- function(input, output, session) {
  modules <- list.dirs("./modules", recursive = FALSE)
  shinypal_setup(input, output, session, modules)
}
}
}
\seealso{
Other app setup:
\code{\link[=shinypal_ui]{shinypal_ui()}}
}
\concept{app setup}
