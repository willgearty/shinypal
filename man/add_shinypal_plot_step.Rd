% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_step.R
\name{add_shinypal_plot_step}
\alias{add_shinypal_plot_step}
\title{Register a complete plot-producing step}
\usage{
add_shinypal_plot_step(
  ind,
  plot,
  fun_workflow,
  fun_report,
  libs = character(0),
  code_guard = NULL,
  output_prefix = "plot_",
  select_dataset = TRUE,
  column_ids = character(0)
)
}
\arguments{
\item{ind}{The index of the step.}

\item{plot}{A \code{\link[shinymeta:metaRender2]{shinymeta::metaRender2()}} object that renders the plot.}

\item{fun_workflow}{A function that generates the UI elements for the
workflow.}

\item{fun_report}{A function that generates the UI elements for the report.}

\item{libs}{A character vector of R packages required for this step.}

\item{code_guard}{An optional zero-argument function evaluated inside the
code output before \code{\link[=get_chunk]{get_chunk()}}. Use it to surface step-specific
\code{validate()} messages.}

\item{output_prefix}{The prefix for the plot's output slot, combined with
\code{ind} to form the id (default \code{"plot_"}, giving \verb{plot_<ind>}). Must match
the \code{\link[shiny:plotOutput]{shiny::plotOutput()}} id used in \code{fun_report} (e.g. \code{"map_"}).}

\item{select_dataset}{Whether the step consumes an upstream dataset (and thus
needs a \code{\link[=df_select_observe]{df_select_observe()}} to keep its dataset dropdown current).}

\item{column_ids}{A character vector of column-selector input ids to keep in
sync via \code{\link[=column_select_observe]{column_select_observe()}} (one per \code{\link[=select_column_input]{select_column_input()}}).}
}
\value{
Called for its side effects and returns \code{NULL} invisibly. Assigns
the rendered plot to \verb{output[[<output_prefix><ind>]]}, renders the step's
generated code, registers the step via \code{\link[=add_shinypal_step]{add_shinypal_step()}}, and (when
requested) installs the dataset/column selector observers.
}
\description{
Convenience wrapper around the boilerplate shared by every step that
renders a plot: it assigns the rendered plot to its output slot, renders
the step's generated code, registers the step via \code{\link[=add_shinypal_step]{add_shinypal_step()}},
and (optionally) keeps the dataset dropdown and column selectors in sync. A
module only supplies the \code{plot} render and its UI/report functions.

Unlike \code{\link[=add_shinypal_data_step]{add_shinypal_data_step()}}, a plot step does not store an
intermediate dataset or show a data-preview modal; its result is a figure,
not a selectable data.frame.
}
\examples{
\dontrun{
# inside a module's server.R
ind <- next_step_index()
p <- shinymeta::metaRender2(shiny::renderPlot, {
  df <- get_int_data(input[[paste0("dataset_", ind)]])()
  shinymeta::metaExpr(plot(df))
})
add_shinypal_plot_step(
  ind, plot = p,
  fun_workflow = function(ind) accordion_panel_remove_button(ind, "Plot"),
  fun_report = function(ind) shiny::plotOutput(paste0("plot_", ind))
)
}
}
\seealso{
Other workflow steps:
\code{\link[=add_shinypal_data_step]{add_shinypal_data_step()}},
\code{\link[=add_shinypal_step]{add_shinypal_step()}},
\code{\link[=next_step_index]{next_step_index()}}
}
\concept{workflow steps}
